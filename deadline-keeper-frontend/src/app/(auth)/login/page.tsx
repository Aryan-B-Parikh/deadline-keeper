'use client';
import { useState } from 'react'; import { useAuth } from '@/lib/auth'; import { useRouter } from 'next/navigation'; import { AlertCircle, ArrowRight } from 'lucide-react'; import Link from 'next/link';
export default function LoginPage(){const {signIn,signInWithGoogle}=useAuth();const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setLoading(true);try{await signIn(email,password);router.push('/dashboard');}catch(err:unknown){setError(err instanceof Error?err.message:'Login failed');}finally{setLoading(false);}};const input='w-full px-4 py-3 bg-surface-inset border border-border-strong rounded-2xl text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand/20 focus:border-brand shadow-neu-inset';return <div className="min-h-screen flex items-center justify-center bg-background px-4 relative overflow-hidden"><div className="absolute top-10 left-10 w-40 h-40 rounded-full bg-clay-pink/35 blur-3xl pointer-events-none"/><div className="absolute bottom-10 right-10 w-48 h-48 rounded-full bg-clay-blue/30 blur-3xl pointer-events-none"/><div className="max-w-[420px] w-full relative z-10"><div className="text-center mb-7"><div className="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-clay-peach/65 text-brand text-2xl mb-4 shadow-neu">⏰</div><h1 className="text-3xl font-bold text-text-primary tracking-tight">DeadlineKeeper</h1><p className="text-text-secondary mt-2">Never miss a deadline again</p></div><div className="clay-surface rounded-[30px] p-6 sm:p-8"><h2 className="text-xl font-bold text-text-primary mb-6">Sign in to your account</h2>{error&&<div className="flex items-start gap-2 pastel-pink text-danger text-sm rounded-2xl p-3 mb-6"><AlertCircle className="w-4 h-4 shrink-0 mt-0.5"/><p>{error}</p></div>}<form onSubmit={handleSubmit} className="space-y-5"><div><label className="block text-sm font-semibold text-text-secondary mb-2">Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required className={input} placeholder="you@example.com"/></div><div><label className="block text-sm font-semibold text-text-secondary mb-2">Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required className={input} placeholder="••••••••"/></div><button type="submit" disabled={loading} className="clay-button w-full min-h-[48px] bg-brand text-white rounded-2xl font-bold flex items-center justify-center gap-2 disabled:opacity-50">{loading?'Signing in...':'Sign in'}{!loading&&<ArrowRight className="w-4 h-4"/>}</button></form><div className="relative my-6"><div className="absolute inset-0 flex items-center"><div className="w-full border-t border-border-strong"/></div><div className="relative flex justify-center text-xs uppercase tracking-wider font-bold"><span className="bg-surface px-3 text-text-muted">or continue with</span></div></div><button onClick={()=>signInWithGoogle()} className="clay-button w-full flex items-center justify-center gap-3 bg-surface min-h-[44px] rounded-2xl font-semibold text-text-primary">Google</button><p className="text-center text-sm text-text-secondary mt-7">Don&apos;t have an account? <Link href="/register" className="text-brand font-bold">Sign up</Link></p></div></div></div>}
