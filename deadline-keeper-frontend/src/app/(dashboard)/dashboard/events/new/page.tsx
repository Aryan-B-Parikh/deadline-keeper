'use client';

import { useState, useRef, useEffect } from 'react';
import { eventApi, type ExtractionResult, type ExtractedEvent, type ReminderInput } from '@/lib/api';
import { ExtractionPreview } from '@/components/ExtractionPreview';
import { ReminderConfig } from '@/components/ReminderConfig';
import { useRouter } from 'next/navigation';
import { cn } from '@/lib/utils';
import { Edit3, Image as ImageIcon, FileText, Loader2, Calendar } from 'lucide-react';

type InputTab = 'manual' | 'screenshot' | 'paste';
export default function NewEventPage() {
  const router=useRouter(); const [tab,setTab]=useState<InputTab>('manual'); const [title,setTitle]=useState(''); const [type,setType]=useState('other'); const [dueAt,setDueAt]=useState(''); const [timezone,setTimezone]=useState('UTC'); const [reminders,setReminders]=useState<ReminderInput[]>([{offsetSeconds:604800,channel:'in_app'},{offsetSeconds:86400,channel:'in_app'},{offsetSeconds:7200,channel:'in_app'}]); const [notes,setNotes]=useState(''); const [extracting,setExtracting]=useState(false); const [extractionResult,setExtractionResult]=useState<ExtractionResult|null>(null); const [pastedText,setPastedText]=useState(''); const [selectedFile,setSelectedFile]=useState<File|null>(null); const fileInputRef=useRef<HTMLInputElement>(null);
  useEffect(()=>{setTimezone(Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC');},[]);
  const handleManualSubmit=async(e:React.FormEvent)=>{e.preventDefault();try{await eventApi.create({title:title.trim(),type,dueAt:new Date(dueAt).toISOString(),timezone,reminders,notes:notes.trim()||null});router.push('/dashboard');}catch(err:unknown){alert(err instanceof Error?err.message:'Failed to create event');}};
  const handleScreenshotExtract=async()=>{if(!selectedFile)return;setExtracting(true);try{const f=new FormData();f.append('screenshot',selectedFile);setExtractionResult(await eventApi.extract(f));}catch(err:unknown){alert(err instanceof Error?err.message:'Extraction failed');}finally{setExtracting(false);}};
  const handleTextExtract=async()=>{if(!pastedText.trim())return;setExtracting(true);try{const f=new FormData();f.append('pastedText',pastedText);setExtractionResult(await eventApi.extract(f));}catch(err:unknown){alert(err instanceof Error?err.message:'Extraction failed');}finally{setExtracting(false);}};
  const handleConfirmExtraction=async(events:ExtractedEvent[])=>{try{await eventApi.confirmExtract({events:events.map(event=>({title:event.title,type:event.type,dueAt:event.dueAt,timezone:event.timezone,reminders:[{offsetSeconds:604800,channel:'in_app'},{offsetSeconds:86400,channel:'in_app'},{offsetSeconds:7200,channel:'in_app'}]})),sourceType:tab==='screenshot'?'screenshot':'pasted_text'});router.push('/dashboard');}catch(err:unknown){alert(err instanceof Error?err.message:'Failed to save events');}};
  const inputClasses='w-full px-4 py-3 bg-surface-inset border border-border-strong rounded-2xl text-text-primary placeholder:text-text-muted focus:outline-none focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all shadow-neu-inset'; const labelClasses='block text-sm font-semibold text-text-secondary mb-2 ml-1';
  if(extractionResult)return <div className="max-w-2xl mx-auto"><ExtractionPreview events={extractionResult.events} clarificationQuestion={extractionResult.clarificationQuestion} onConfirm={handleConfirmExtraction} onCancel={()=>setExtractionResult(null)}/></div>;
  return <div className="max-w-2xl mx-auto space-y-7"><div className="flex items-center gap-3"><div className="w-11 h-11 rounded-2xl clay-inset text-brand flex items-center justify-center"><Calendar className="w-5 h-5"/></div><div><h1 className="text-2xl font-bold text-text-primary tracking-tight">Add Deadline</h1><p className="text-sm text-text-secondary">Track a new assignment or event</p></div></div>
    <div className="clay-surface rounded-[28px] overflow-hidden relative">{extracting&&<div className="absolute inset-0 z-50 bg-surface/75 backdrop-blur-sm flex flex-col items-center justify-center rounded-[28px]"><Loader2 className="w-8 h-8 text-brand animate-spin mb-3"/><p className="font-bold text-text-primary">Extracting deadlines...</p><p className="text-sm text-text-secondary">Using AI to analyze your input</p></div>}
      <div className="flex border-b border-border-subtle overflow-x-auto bg-surface-hover/70 scrollbar-hide">{[{key:'manual',label:'Manual Entry',icon:Edit3},{key:'screenshot',label:'Screenshot',icon:ImageIcon},{key:'paste',label:'Paste Text',icon:FileText}].map(({key,label,icon:Icon})=><button key={key} type="button" onClick={()=>setTab(key as InputTab)} aria-selected={tab===key} role="tab" className={cn('flex-1 flex items-center justify-center gap-2 min-h-[52px] px-6 text-sm font-bold border-b-2 whitespace-nowrap transition-all',tab===key?'border-brand text-brand bg-clay-lavender/35 shadow-neu-inset':'border-transparent text-text-secondary hover:text-text-primary hover:bg-surface-hover')}><Icon className="w-4 h-4"/>{label}</button>)}</div>
      <div className="p-6 sm:p-8">{tab==='manual'&&<form onSubmit={handleManualSubmit} className="space-y-5"><div><label htmlFor="event-title" className={labelClasses}>Title *</label><input id="event-title" type="text" value={title} onChange={e=>setTitle(e.target.value)} required maxLength={200} className={inputClasses} placeholder="e.g., CS101 Final Exam"/></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-5"><div><label htmlFor="event-type" className={labelClasses}>Type *</label><select id="event-type" value={type} onChange={e=>setType(e.target.value)} className={inputClasses}><option value="exam">📝 Exam</option><option value="submission">📋 Submission</option><option value="hackathon">💻 Hackathon</option><option value="other">📌 Other</option></select></div><div><label htmlFor="event-due-at" className={labelClasses}>Due At *</label><input id="event-due-at" type="datetime-local" value={dueAt} onChange={e=>setDueAt(e.target.value)} required className={inputClasses}/></div></div><div><label className={labelClasses}>Reminders</label><div className="clay-inset rounded-2xl p-4"><ReminderConfig value={reminders} onChange={setReminders}/></div></div><div><label htmlFor="event-notes" className={labelClasses}>Notes</label><textarea id="event-notes" value={notes} onChange={e=>setNotes(e.target.value)} rows={3} maxLength={2000} className={cn(inputClasses,'resize-none')} placeholder="Any additional notes or links..."/></div><div className="flex flex-col-reverse sm:flex-row justify-end gap-3 pt-4 border-t border-border-subtle"><button type="button" onClick={()=>router.push('/dashboard')} className="clay-button px-5 min-h-[44px] rounded-2xl text-text-primary font-semibold">Cancel</button><button type="submit" className="clay-button px-6 min-h-[44px] bg-brand text-white rounded-2xl font-bold">Save Deadline</button></div></form>}{tab==='screenshot'&&<div className="space-y-6"><div className="clay-inset border-2 border-dashed border-border-strong rounded-2xl p-10 text-center hover:border-brand transition-colors group"><input ref={fileInputRef} type="file" accept="image/*" onChange={e=>setSelectedFile(e.target.files?.[0]??null)} className="hidden" id="screenshot-upload"/><label htmlFor="screenshot-upload" className="cursor-pointer block"><div className="w-16 h-16 bg-clay-blue/40 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-neu-inset"><ImageIcon className="w-8 h-8 text-brand"/></div>{selectedFile?<p className="text-brand font-bold break-all px-4">{selectedFile.name}</p>:<><p className="text-text-primary font-bold text-lg mb-1">Click to upload screenshot</p><p className="text-sm text-text-secondary">PNG, JPEG or other image formats</p></>}</label></div><div className="flex justify-end pt-4 border-t border-border-subtle"><button type="button" onClick={handleScreenshotExtract} disabled={extracting||!selectedFile} className="clay-button px-6 min-h-[44px] bg-brand text-white rounded-2xl font-bold disabled:opacity-50">{extracting?'Extracting...':'Extract Deadlines'}</button></div></div>}{tab==='paste'&&<div className="space-y-6"><textarea id="deadline-text" value={pastedText} onChange={e=>setPastedText(e.target.value)} rows={8} maxLength={20000} className={cn(inputClasses,'resize-none font-mono text-sm leading-relaxed')} placeholder={'Paste syllabus text or email here...\n\ne.g., "The final project submission is due on December 15th at 11:59 PM PST"'}/><div className="flex justify-end pt-4 border-t border-border-subtle"><button type="button" onClick={handleTextExtract} disabled={extracting||!pastedText.trim()} className="clay-button px-6 min-h-[44px] bg-brand text-white rounded-2xl font-bold disabled:opacity-50">{extracting?'Extracting...':'Extract Deadlines'}</button></div></div>}</div>
    </div>
  </div>;
}
